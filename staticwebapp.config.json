{
  "routes": [
    {
      "route": "/*",
      "rewrite": "/api/edge"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "*.jpg", "*.jpeg", "*.png", "*.svg", "*.ico", "*.webp"]
  }
}